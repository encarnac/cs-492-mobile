import 'dart:math';

class CallPrediction {
  final Random _random = Random();
  final String prediction;

  CallPrediction({this.prediction = 'Nah'});

  // Uses the String at a random index and sets it as the prediction
  // set? void?
}
